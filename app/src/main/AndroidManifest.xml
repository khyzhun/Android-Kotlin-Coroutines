<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sashakhyzhun.kotlincoroutines">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".trash.DifferentRestApproachesActivity" />
        <activity android:name=".trash.ThreadsVsCoroutines" />
        <activity android:name=".trash.ChannelExampleActivity" />
        <activity android:name=".CoroutineBasicsActivity" />
        <activity android:name=".CancellationAndTimeoutsActivity" />
        <activity android:name=".ComposingSuspendingFunctionsActivity" />
        <activity android:name=".CoroutineContextAndDispatchersActivity" />
        <activity android:name=".ChannelsActivity" />
        <activity android:name=".SharedMutableStateAndConcurrency" />
        <activity android:name=".SelectExpression" />
    </application>

</manifest>